// js/calendar.js - Script para calendário simples

function initCalendar(reservas) {
    const calendarEl = document.getElementById('calendar');
    // Implementação simples de calendário
    // Para um calendário completo, considere usar uma biblioteca como FullCalendar
    calendarEl.innerHTML = '<p>Calendário em desenvolvimento. Reservas: ' + reservas.length + '</p>';
    // Aqui você pode integrar uma biblioteca de calendário JS
}